# golang-vuetify-sample

golang + vuetifyjs sample.

## install

- install requres

```
$ npm install -g concurrently
$ npm install -g lite-server
```

- run go server

```
$ go run main.go
```

- run web app

```
$ cd web
$ yarn install
$ yarn start
```
